#include <stdio.h>

int main() {
    // deklaracja i inicjalizacja zmiennej num
    int num = 1000;

    // drukowanie wartości zmiennej num
    printf("%d jest wartością zmiennej num\n", num);

    // zwrócenie wartości 0, co oznacza poprawne zakończenie programu
    return 0;
}
